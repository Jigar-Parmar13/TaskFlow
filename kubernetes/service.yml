apiVersion: v1
kind: Service
metadata:
  name: taskflow-service
  namespace: taskflow
spec:
  selector:
    app: taskflow

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: LoadBalancer 